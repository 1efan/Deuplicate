title: Deuplicate
description: Split off player-like copies of yourself that fight, guard, and gather for you.
theme: jekyll-theme-cayman
defaults:
  - scope:
      path: ""
    values:
      layout: default
